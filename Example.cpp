#include "Example.h" //Can use all declarations/definitions in Example.h header file

//Definition for setNumber (declared in Example.h)
void Example::setNumber(int first, int second){ // Must state in scope of Example
    number1 = first;
    number2 = second;
}